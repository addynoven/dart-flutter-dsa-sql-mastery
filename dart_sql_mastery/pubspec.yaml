name: dart_sql_mastery
description: Interactive SQLite and Raw SQL Mastery package for Dart & Flutter Developers.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  sqlite3: ^2.4.0
  path: ^1.9.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
