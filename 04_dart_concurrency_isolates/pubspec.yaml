name: dart_concurrency_isolates
description: Dart Concurrency, Event Loop, Futures, Streams, and Isolates Multithreading Mastery.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  async: ^2.11.0
  crypto: ^3.0.3

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
